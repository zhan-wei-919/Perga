# Perga 进度快照 — 2026-05-24

这份文档是滚动更新的进度快照:每轮工作结束就地更新,并把文件名改成当天日期,方便下次开工直接续上。

架构原始设计见 [`web-terminal-architecture.md`](web-terminal-architecture.md)。运行时 / 代码规范在仓库根 [`CLAUDE.md`](../CLAUDE.md)。

---

## 1. 总体目标

Perga 是一个**以 Web 前端为主**、Rust 后端接管 PTY 和终端协议解析的新形态终端。终端字节流由后端 alacritty 内核解析,前端只消费规范化后的协议事件(`Init` / `Patch` / `Exited`)。

最终形态(目标态):

- 桌面 Tauri app:本地 PTY + russh,所有 auth 本地。
- iPad / Android Tauri app:russh 直连远程机器(平板沙箱不允许 spawn ssh 子进程)。
- 中间形态(当前开发态):**纯 Web,前端浏览器 ↔ 本机 Rust server**,绕开 Tauri 调试摩擦。

用户给出的 5 个产品需求:

1. 好看,自定义布局。
2. 多 SSH 会话。
3. **未来重要场景是 iPad / Android 远程 SSH 连一台机器**。这反过来要求 SSH 走 russh,不走 `ssh` 子进程。
4. 分块布局 = **tabs(顶部)+ panes(tab 内分屏)+ Warp 风格命令 block(完成的命令收成 DOM 块,但当前行仍是 inline 终端体验,不是固定底部输入框)**。
5. 自定义缩放百分比。

---

## 2. 架构总览

```text
┌─ Rust backend ────────────────────────────────────────┐
│                                                       │
│  pty              ─ 子进程 + PTY,sync + crossbeam     │
│  terminal-engine  ─ alacritty 适配,Snapshot 模型      │
│  terminal-protocol─ Init/Patch/Exited,RLE 行编码      │
│  terminal-input   ─ key/mouse/paste → PTY bytes       │
│  terminal-session ─ 缝合 4 层,events()/input() handle│
│                                                       │
│  perga-cli                                            │
│    ├─ bin "perga"        交互式 PTY demo (raw mode)   │
│    └─ bin "perga-bench"  sync 路径性能基准            │
│                                                       │
│  perga-server     ─ axum + tokio,WS /ws?rows=&cols=  │
│    bridge.rs:唯一 sync↔async 缝合点(OS 线程 +        │
│    crossbeam→tokio mpsc),严格保 PTY/engine 路径同步  │
│                                                       │
└───────────────────────────────────────────────────────┘
                       │ WebSocket (text JSON frames)
                       │ 协议:server→client = ProtocolEvent
                       │       client→server = ClientMessage
                       ▼
┌─ Web frontend (Vite + Solid + TS) ────────────────────┐
│                                                       │
│  state/protocol.ts    协议出站 TS 类型(与 Rust 对齐)│
│  state/wire.ts        协议入站 TS 类型                │
│  state/session.ts     纯函数 reducer + RLE 解码        │
│  state/session_store.ts  Solid createStore + produce  │
│  net/ws.ts            SessionSocket(单连接,无重连)│
│  render/metrics.ts    字体度量(hidden span 法)       │
│  render/palette.ts    Color → CSS(16色+256色 cube)   │
│  render/grid_dom.tsx     DOM 行渲染,run-grouping 优化 │
│  input/keyboard.ts    DOM KeyboardEvent → ClientMsg   │
│  input/resize.ts      ResizeObserver + 80ms debounce  │
│  ui/app.tsx           全屏单 session 装配              │
│  ui/perf_overlay.tsx  ?perf=1 启用,右上角实时指标   │
│  util/perf.ts         浏览器侧采样器                  │
│                                                       │
└───────────────────────────────────────────────────────┘
```

关键约束(出自 CLAUDE.md):

- PTY / engine / session 线程**严格 sync + crossbeam**,**不**进 tokio runtime。
- tokio 只在 `perga-server` 这一 crate 内作为 side pool。`bridge.rs` 是整个项目里唯一一处 sync 转 async 的位置。
- 协议数据先行(数据模型优先于特殊情况);不为未来写代码;边界验证;不静默吞错。

---

## 3. 阶段规划总览

完整初始 plan 在 `/home/zhanwei/.claude/plans/federated-hugging-pie.md`(Phase 0/1 的设计文档)。后续阶段的具体规划在 §6-§10 详细展开。

| Phase | 主题 | 状态 | 预估 | 详细规划 |
|---|---|---|---|---|
| **0** | server transport(axum + WS) | ✅ 完成 | — | — |
| **1** | 前端 MVP 单 session | ✅ 完成 | — | — |
| **2** | Tabs + Panes(多 session 多分屏) | ✅ 完成 | — | §6 |
| **3** | OSC 133 + Warp 风格命令块 | ✅ 完成,后移除 | — | §7 |
| **4** | Zoom + 主题 + 命令块重做 + 视觉抛光 | ✅ 完成 | — | §8 |
| **4R** | 移除命令块 → 普通滚动终端 + OSC 133 失败标记 | ✅ 完成 | — | §5 |
| **5** | SSH (russh) + host profile | ✅ 完成 | — | §9 |
| **6** | Tauri 打包(Linux 桌面 + 移动端 UX 架构) | ✅ 完成 | — | §10 / §5 |
| **6.5** | Mobile bundle(Android / iOS)+ icon / signing / Win/Mac bundle | ⏸ 未开始 | — | TODO.md |
| **7+** | 终端选择复制 / Mouse / IME / 主题自定义 / WS 重连 / scrollback 持久化 | ⏸ 长尾 | — | §11 |

---

## 4. 当前进度 — 工程细节

### 已完成 — Phase 0 server

- `crates/perga-server`:`/ws?rows=R&cols=C` WS 端点,bind `127.0.0.1:7777`,Ctrl-C 优雅退出。
- `ClientMessage` 入站枚举(key/paste/mouse/focus/resize)+ 单元测试 5 个。
- `bridge.rs` 一条 OS 线程把 crossbeam Receiver 翻译到 tokio mpsc。
- `tests/lifecycle.rs`:3 个端到端集成测试(Init 帧 / 非法 size / 键盘 round-trip)。

### 已完成 — Phase 1 frontend

- Vite + Solid + TS 脚手架,jsdom 测试环境,WS 代理 `/ws → 127.0.0.1:7777`。
- 协议 TypeScript 类型与 Rust serde 形状对齐;`expandRowEntries` 把 RLE 展开成 `Cell[][]` 用于 Canvas 绘制。
- 纯函数 reducer + Solid `createStore` + `produce`:Patch 走行级 mutate,获得细粒度信号 → 只有命中 dirty row 的 `rowGen` 信号失效。
- Canvas Grid renderer:DPR-aware,订阅 `state.rowGen[r]` 增量重绘,光标 reverse 绘制。
- 键盘编码:DOM KeyboardEvent → `ClientMessage::Key`,丢 modifier-only / IME composing / F13+。
- ResizeObserver + 80ms trailing debounce(同尺寸去重)。
- 22 个 vitest 单测覆盖 RLE 三种 entry + 特殊键 + 修饰符。
- `pnpm exec vite build`:bundle 27 KB,gzipped 10 KB。

### 已完成 — 性能工具链

由用户反馈"`ls` 有延迟"触发,加了三件工具:

| 工具 | 路径 | 用法 |
|---|---|---|
| 后端 sync 路径基准 | `crates/perga-cli/src/bin/bench.rs` | `cargo run --release -p perga-cli --bin perga-bench` |
| WS 端到端 RTT 测试 | `crates/perga-server/tests/rtt.rs` | `cargo test --release -p perga-server --test rtt -- --nocapture` |
| 浏览器 perf overlay | `web/src/util/perf.ts` + `web/src/ui/perf_overlay.tsx` | URL 加 `?perf=1` |

### 已完成 — Canvas run-grouping 优化

用户反馈 `dispatch p99 = 62ms`(在 release 后端 + release 前端下)。定位到 `drawCell` 每个 cell 都设 `fillStyle` 两次 + `font` 一次,80×24 = 1920 cells × 3 状态切换 = ~5760 canvas ops。

重写 `drawRow`:

- **run-grouping**:同 `(fg, bg, attrs)` 的连续单宽 cell 合并为一段 run,1 次 `fillRect` 画整段背景 + 1 次 `fillText` 画整段文字。
- **状态缓存**:`lastFillStyle` / `lastFont` 缓存,相同值不下发 setter。
- Wide char、cursor、wide_spacer 走单独的 `drawSingleCell` 回退路径。

期望 80 列全屏重绘从 ~400 canvas ops 降到 ~6-12 ops/行。run-grouping 压低了每行成本,但当时没发现「每个 patch 都在全屏重绘」这个更上游的 bug(见下一节)。

### 已完成 — 前端 dispatch 性能优化(本轮)

run-grouping 后 dispatch p99 仍有 44ms。定位到三层根因并逐一修掉:

1. **`syncCanvasSize` 比较 bug**:`grid_canvas.tsx` 的 canvas 尺寸检查写成
   `Math.round(X) !== X`,对任意非整数 X 恒为真 → **每个 patch 都全屏重绘**、
   脏行缓存被清空,增量重绘形同虚设。修成两边都比 round 后的目标值。
2. **grid 移出 Solid store**:`SessionState.grid` 拆成 store 外的 raw `Cell[][]`
   backing buffer,渲染热路径不再穿 Solid proxy。
3. **RAF 合批**:`grid_canvas.tsx` 把脏行收进 `pendingRows`,合并到一帧 RAF 统一画。

`ls` ×100 autotest 回测:

| 指标 | 改造前 | 现在 |
|---|---|---|
| dispatch p99 | 43.8 ms | **0.4 ms** |
| dispatch p50 | 16.6 ms | **0.1 ms** |
| latency enter→settle p99 | 143.9 ms | **4.3 ms** |
| render canvas RAF p99 | (并入 dispatch) | 1.1 ms |

大幅超过「p99 < 10ms」目标。原计划的后端 coalescing / 协议 scroll op 触发条件是
「p99 仍 > 10ms」,未命中 → 不做(不为未来写代码)。

### 已完成 — Phase 2 tabs + panes(本轮)

多 tab + tab 内二叉分屏。**后端零改动** —— 每条 WS 已独立 spawn session。

- 新增纯模型层:`state/pane_tree.ts`(二叉分屏树 + 纯变换)、
  `input/workspace_shortcuts.ts`(快捷键纯匹配器)。
- 新增响应式层:`state/workspace.ts`(`Workspace` 容器 + `LeafSession` 注册表 ——
  树是值、session 是资源,按 leaf id 关联)。
- 新增 UI:`ui/pane_leaf.tsx`(单 pane 装配,从旧 `app.tsx` 抽出)、
  `ui/pane_tree_view.tsx`(递归 flex 布局 + 2px gutter 拖拽)、`ui/tab_bar.tsx`。
- 重写 `ui/app.tsx` 为 workspace root + capture 阶段快捷键拦截器;
  `render/metrics.ts` 加 `cellsForBox` 去重「容器尺寸 → 网格」公式。
- 测试:`pane_tree` / `workspace` / `workspace_shortcuts` 三个新 spec,共 73 测试全绿。

关键决策:**无空态不变量** —— `PaneTree` 永不为空、每个 leaf 永远挂活 session,
关掉最后一个 pane / tab 直接 respawn,消除 nullable 特殊情况(决策 B);workspace
快捷键由 app 根 capture 阶段拦截,`stopPropagation` 后不漏进 PTY。

实现后经一轮 code review + 实测共修四处:autotest RAF pending 计数泄漏、pane 卸载
未补发 focus lost、workspace 缺生命周期测试,以及一个 `props.session` 响应式 getter
在组件卸载时重算抛错、打断 Solid 同步更新导致整个 app 卡死的回归。UI 调整:去掉
focused pane 的蓝色边框、gutter 收细到 2px(视觉量待 Phase 4 settings 面板可配置,
见 `TODO.md`)。

---

## 5. 当前进度 / 下一步(下次接上看这条)

Phase 0 / 1 / 2 / 3 都已完成。

**Phase 3 已完成并经 review 加固(2026-05-21)**:OSC 133 shell 集成 + Warp
风格命令块落地 —— 实现细节见 §7,设计决策见 §12,已知限制见 §13。

落地后又过了**三轮外部 code review**,修掉约十个 bug,全部已修 + 补测试:

- alt-screen 边界:同 chunk 内进出、退出与新 prompt 同 chunk;旁路解析器现在
  也认 alt-screen 进入序列(`CSI ?1049h` 等)当切点。
- 命令输出无结尾换行(`printf hi`)丢内容 → 引擎记 D 的行+列,补回截断行。
- `printf hi` 那一行被命令块和活动区共用 → `snapshot` 抹掉已归块的前缀列,
  Canvas 不重复画。
- `CSI 3J` 清 scrollback 时 `history_size` 下溢 → `advance_alacritty` 检测
  回落并重新基准化。
- bash 钩子:`C` 改走 `PS0` 不走 DEBUG trap(否则和 starship/direnv 的
  `PROMPT_COMMAND` 冲突);`PROMPT_COMMAND` 数组形态保形 prepend。
- `grid_canvas` 单独跟踪 `activeTop`,变了就整屏重画。
- `perga raw-debug` 补 `drain_marks` → 与正式路径一致。

**验证状态**:`cargo fmt/clippy/test --workspace`、前端 `tsc` + `vitest`、
`perga raw-debug` 实跑 —— 全绿。Phase 3 的浏览器视觉手测(§7 验收的 DOM block
长相 / Canvas 裁剪 / 自动滚手感)也已手过一遍,通过。Phase 3 改动已 commit
(`57c08e0 feat: add OSC 133 command blocks`)。

### 已完成 — shell 集成自动注入(2026-05-22)

把 Phase 3 留的「用户要手动 `source` 集成脚本」打磨项做掉了 —— 后端 spawn
shell 时自动注入 OSC 133 集成,用户无感(对齐 VS Code / iTerm2 / WezTerm)。

- 新 `crates/pty/src/shell_inject.rs`:`inject_shell_integration(&mut PtyConfig)`
  按 `cfg.program` 识别 shell。bash 走 `--rcfile`(生成的 rcfile 先转源
  `~/.bashrc` 再挂集成);zsh 走 `ZDOTDIR` 重定向(Perga 目录里的
  `.zshenv` / `.zshrc` 转源用户配置 + 还原 ZDOTDIR + 挂集成)。集成脚本
  `scripts/perga-{bash,zsh}.sh` 用 `include_str!` 编译进二进制,运行时不
  依赖仓库 / 安装布局。集成文件落地 `~/.perga/shell/`。
- 非 bash / zsh、或写文件失败 → best-effort 降级到纯终端(就是 Phase 3
  之前的正常行为,有测试、被支持,不是掩盖 bug)。集成文件原子写(临时
  文件 + `rename`),挡住 server 并发 spawn 的截断读。
- 三个 spawn 入口(`ws.rs` / `main.rs` / `raw_debug.rs`)各加一行注入调用。
- 测试:`shell_inject` 11 个单测;`perga raw-debug` 实跑确认未手动 `source`
  也能收到 `command_block`(exit 0 + 输出内容正确)。

落地后过了两轮 review,按 bug 修复流程(测试先复现 → 再修)修掉 3 + 1 个 bug:

- **zsh 用户在 `.zshenv` 里 `export ZDOTDIR`** → 注入失效。注入的 `.zshenv`
  转源用户 `.zshenv` 后没把 ZDOTDIR 拉回 Perga 目录,zsh 转去用户目录读
  `.zshrc`,集成不加载。修:`.zshenv` 记下用户最终 ZDOTDIR 到
  `PERGA_USER_ZDOTDIR`,再 re-pin ZDOTDIR 回 Perga 目录。
- **`__perga_precmd` 吞掉 `$?`**。它 prepend 在用户 PROMPT_COMMAND / precmd
  hook 前,函数返回值是 `printf` 的状态(恒 0),后续 starship / direnv 等
  hook 看到的 `$?` 全变 0,失败命令显示成成功。修:`local` 存退出码、emit
  后 `return` 它(bash + zsh 同改)。
- **`perga raw-debug` 管道 stdin 下卡死**(pre-existing,git stash 复测确认
  非本次回归)。host stdin EOF 时只发一次 Ctrl-D,会和 shell 启动抢跑 ——
  shell 切 readline raw mode 前,canonical 行规把 0x04 当 VEOF 吃掉、丢失。
  修:`raw_debug.rs` 周期补发 Ctrl-D,直到某一发落在 shell 空 prompt 上、
  shell 自己退出。
- **(二轮 review)上一条的初版修复加过「久无输出 → 判定卡死 → kill」的
  兜底,会误杀正在跑静默长命令(`sleep`)的 shell**。已去掉:**不设**输出
  idle 超时 —— shell 真卡死(用户 pipe 死循环)就让它挂着、由用户 Ctrl-C。
  另:`raw_debug.rs` 回归测试显式 `SHELL=/bin/bash`,不受运行环境 `$SHELL`
  影响。

### 已完成 — Phase 4:Zoom + 主题 + 命令块重做 + 视觉抛光(2026-05-22)

纯前端,后端零改动。用户在讨论中把 §8 原计划扩了范围,实际交付:

- **自定义缩放**:全局 zoom 50–200%(步进 10)。`Ctrl+Shift+= / − / 0` +
  设置面板滑块。`settings.ts` 持有 `effectiveFontSize`;`pane_leaf` reactive
  重测 metrics、重建 resize watcher、主动补发 resize(zoom 不改容器尺寸,
  `ResizeObserver` 不会自己 fire);`grid_canvas` 字号 / 主题变化触发整屏失效。
- **深 / 浅双主题**:`render/theme.ts` 一张 `THEMES` 表,`applyTheme` 写
  `:root` CSS 变量。颜色解析二分 —— DOM 命令块走 `colorToDomCss`(`var()`,
  切主题零重渲染),Canvas 走 `colorToCanvasCss`(具体色,`fillStyle` 不接受
  `var()`)。
- **设置面板**:tab 栏最右齿轮 → 居中 modal,含 zoom 滑块 / 主题 / 命令块
  风格。`settings.ts` 持久化到 localStorage,启动校验回落。
- **命令块 Warp 风格重做**:三种风格(彩色竖条卡片 / 状态色命令行卡片 /
  极简分隔线)由设置切换。修掉原 `padding:1px` 极挤、输出区背景与 pane 同色
  「块不成块」的问题。
- **命令块右键菜单**:`context_menu.tsx` 通用菜单,复制命令 / 输出 / 全部 /
  折叠。`Ctrl+C` 有文本选区时走浏览器复制不发 SIGINT。
- **tab 栏自动隐藏**:单 tab 隐藏(`position:absolute` + translateY)、hover
  顶部触发条滑出;≥2 tab 常显。单 tab 的 hover 显隐不触发 resize。
- **非焦点 pane 变暗**:多 pane 焦点视觉指示(`opacity`)。
- 设置经 `SettingsContext` 跨递归 pane 树下发,叶子组件直接 `useSettings()`。
- 测试:`theme` / `settings` / `zoom_shortcuts` 三个新 spec + `cellsToText`,
  workspace 共 131 前端测试全绿;`tsc` + `vite build` 通过。

### 已完成 — 移除命令块,改普通滚动终端 + OSC 133 失败标记(2026-05-23)

用户实测后决定弃用 Warp 风格命令块(无输出命令留空块、成功命令一片绿勾的
视觉噪声)。改回**普通滚动终端**,但保留 OSC 133 shell 集成、改用途为「标记
失败命令」。Phase 4 的主题 / 缩放 / 设置面板 / tab 自动隐藏 / 非焦点变暗全部
保留;命令块三风格 / 右键菜单 / `blockStyle` 设置随命令块一起移除。

- **scrollback 改前端累积**:命令块原本兼任 scrollback —— 移除后由前端自己攒。
  协议 `Patch` 加 `scrolled_rows`(本帧滚出 viewport 顶的行,后端读一次转发
  一次)+ `cleared`(`CSI 3J`);删 `active_top`。`CommandBlock` 事件换成精简
  的 `CommandEnd { exit, line }`。
- **渲染**:历史 = 虚拟化 DOM 行列表(`history_view.tsx`,只渲染可见窗口、
  原生可选中复制);活动区 = Canvas(`grid_canvas` 删掉 `active_top` 裁剪,
  画整个 viewport)。`pane_leaf` 容器滚动 `[历史][Canvas]`。
- **失败标记**:`command_end` 的 `exit≠0` → 该命令输入行滚进 history 后,
  左侧 gutter 染红(`history.ts` 的 pending → drain 机制)。成功命令不标。
- **引擎精简**:`shell_integration.rs` 的 `CommandRegion` 收成 `{line,exit}`;
  `engine.rs` 删命令块组装 / `mask_block_columns` / `active_top` / `drain_marks`,
  新增 `take_scrolled_rows` / `scrollback_cleared` / `drain_command_ends`;
  alt-screen 期间 `scroll_total` 冻结。shell 注入 + 脚本保留不动。
- `clear` 现在经 `cleared` 真正清掉历史(原 `TODO.md` 那条已解决)。
- 测试:后端 `osc133.rs` / `marks.rs` 重写;前端新增 `history` / `row_segments`
  spec,`session.spec` 改写。`cargo test` + `clippy` + `vitest`(135)+ `tsc`
  + `vite build` 全绿。

### 已完成 — 输入 / Canvas 渲染修补(2026-05-23)

围绕普通滚动终端的实测问题做了一轮前端局部修补:

- **粘贴快捷键**:`input/paste_shortcuts.ts` 识别 `Ctrl/Cmd+V`、
  `Ctrl/Cmd+Shift+V`、`Shift+Insert`。命中时 `pane_leaf` 不再把它编码成
  `ClientMessage::Key`,也不 `preventDefault`,让浏览器继续派发
  `ClipboardEvent("paste")`;现有 `onPaste` 统一发 `{ type:"paste", text }`
  给后端,复用后端 bracketed paste / paste injection 防护。
- **光标 / 中文残影**:Canvas 小数 cell 宽度 + 字形抗锯齿会在空格移动光标、
  宽字符重绘时留下细残边。修法分三层:
  1. 光标只移动且旧行没变时,重画旧光标所在整行,不再只擦单格。
  2. `drawRow` 每次行级重绘先铺整行默认背景,再按原 run/cell 逻辑画文本和
     自定义背景。
  3. `measureCell` 同时测 latin `M` 和 CJK `你`,若 fallback CJK 字体需要更
     宽的全角格,自动撑大 `cellW`;`fillText` 裁剪在对应 run / cell slot 内,
     宽字符限制在两格范围。
- **复制现状**:历史区是 DOM 行,仍可用浏览器原生 selection 复制;当前活动区是
  Canvas,还没有终端级选择系统,所以当前 prompt / 当前屏内容不能完整复制。
  已记录到 `TODO.md`。
- **鼠标上报现状**:后端 wire / session / `terminal-input::encode_mouse` 已接好,
  前端 TS wire 类型也有 `ClientMessage::Mouse`,但前端还没有 pointer / wheel
  listener 发 mouse 帧。TUI 鼠标交互当前不可用;复制选择实现时要给后续
  mouse reporting 预留边界(见 `TODO.md`)。
- 验证:`pnpm test -- paste_shortcuts.spec.ts copy_shortcuts.spec.ts keyboard.spec.ts`,
  `pnpm test -- grid_canvas.spec.ts metrics.spec.ts`,`pnpm build` 全绿。

### 已完成 — 活动区主渲染切换为 DOM(2026-05-23)

用户确认不保留 Canvas fallback 后,活动区主 renderer 从 Canvas 2D 完整切到
DOM grid,并删除旧 Canvas 模型。

- 新 `web/src/render/grid_dom.tsx`:继续使用 raw `Cell[][]` + `rowGen`,按 dirty
  row RAF 合批更新。每行固定高度,连续同 style cell 合并成 inline-block
  span,不做 per-cell DOM;宽字符单独占 2 cell;cursor 全部走单个 overlay,
  block cursor 移动不触发行 DOM 重建。
- `PaneLeaf` 改成 `[历史 DOM][活动区 DOM grid]`;alt-screen 时仍挂起历史,
  活动区独占。
- 删除 `web/src/render/grid_canvas.tsx` 与 `web/tests/grid_canvas.spec.ts`;
  `palette.ts` 删除 `colorToCanvasCss`,主题切换完全靠 CSS variables。
- `TODO.md` 删掉「活动区主渲染从 Canvas 改成 DOM 行渲染」条目;复制系统条目
  改成基于 DOM grid 的终端 selection model。

验证:`pnpm test`(147 tests)、`pnpm build`、`git diff --check` 通过。

浏览器 autotest 回测(用户本机,`?perf=1`):

| 场景 | latency p99 | dispatch p99 | render RAF p99 | render RAF max |
|---|---:|---:|---:|---:|
| `ls` ×100 | 18.1 ms | 0.7 ms | 3.5 ms | 4.8 ms |
| `cat CLAUDE.md` ×100 | 648.6 ms | 2.8 ms | 5.9 ms | 8.7 ms |

结论:DOM renderer 热路径目前健康。普通交互和中等滚屏输出的 render p99 仍显著
低于 60Hz 的 16.7ms 帧预算;后续移动端风险主要看多 pane 同时高吞吐输出和
偶发 dispatch 长尾。

### 已完成 — Phase 5:SSH(russh)+ host profile(2026-05-24)

iPad / Android 远程场景是产品最终形态(状态快照 §1.3),不能 spawn `ssh`
子进程,故引入 russh 把 SSH 客户端直接编进 app。落地形态:用户在桌面设置面板
列出 `~/.perga/hosts.toml` 中的 host profile,点 Connect 新开 SSH tab。
本地 shell 新建路径(`+` / `Ctrl+Shift+Enter`)**行为完全不变**,SSH 走桌面
side-door。移动端 always-SSH 留给 Phase 6 Tauri mobile 阶段一起做。

**架构主线** —— SSH 内部嵌 mini tokio runtime 驱动 russh,对外暴露和本地
`PtySession` 同形态的 sync crossbeam 接口。`terminal-session::TerminalSession`
内部存 `Box<dyn Transport>`,engine 线程 0 改动:

- **新 `crates/transport`** —— 共享 `TransportCommand`(Write/Resize/Shutdown)/
  `TransportEvent`(Output/Exited/Error)/ `TerminalSize` / `ExitStatus` /
  `Transport` trait。`pty::PtyCommand` 等被替换成 transport 类型,机械重命名
  跨 workspace 扩散。
- **新 `crates/ssh`** —— russh 0.61。独立 OS 线程跑 current_thread runtime,
  `block_on` 一段 async setup(TCP connect → host key TOFU → agent auth →
  channel_open_session → request_pty + request_shell),成功后 spawn shuttle
  task 双向穿梭(crossbeam ↔ russh channel)。Bridge OS 线程把外部 crossbeam
  Sender 转给内部 tokio mpsc。Drop 时 oneshot 通知 shuttle 关闭,join 两条
  内部线程(带 3s 预算,超时 detach + warn)。
- **`terminal-session` 接受 `Box<dyn Transport>`** —— 拆 `spawn_local(PtyConfig)`
  与 `spawn_with_transport(Box<dyn Transport>, size)` 两个入口。引擎线程
  `run_engine_loop` 签名换 transport 类型,内部逻辑 0 改动。
- **server profile store** —— 新 `crates/perga-server/src/profiles.rs` 读
  `~/.perga/hosts.toml`,新 `GET /api/hosts` 端点返回不含 auth 的摘要。
  `WS /ws?profile=<id>` 在 upgrade 前同步 connect + auth + open shell,失败返
  4xx/5xx,前端拿 HTTP 错,不进半死状态。
- **前端 side-door** —— 新 `state/profiles.ts` fetch wrapper(失败降级到 `[]`),
  `settings_panel.tsx` 加"远程主机" section,空列表显示
  「在 ~/.perga/hosts.toml 添加主机后会出现在这里」,非空每条一个 Connect 按钮。
  `state/workspace.ts` 加 `newTabWithProfile(id)`,`LeafSession` 透 `profileId`
  到 `SessionSocket.connect`,`net/ws.ts::buildUrl` 把 profile 拼进 URL。`+` /
  快捷键路径完全不动。

**v1 范围裁剪**(详见 plan + TODO.md):
- Auth:**agent only**(读 `SSH_AUTH_SOCK`)。passphrase 私钥用 agent 解锁。
- host key:**静默 TOFU** on `~/.ssh/known_hosts`。未知自动写入并接受;
  fingerprint 不匹配 **直接 Err 不绕过**(真实安全信号,CLAUDE.md「不过度兜底」)。
- 不做 password / 2FA / jump host / agent forwarding / passphrase key file UI /
  profile CRUD UI / SSH OSC 133 / 移动端 always-SSH。这些都进 `TODO.md`。

**Probe 工具** —— 新 `perga-cli` binary `perga-ssh-probe`:
`cargo run --release -p perga-cli --bin perga-ssh-probe -- --host x --user y`,
不依赖前端 / server / terminal-session,直接构造 `SshSession` 灌一段命令读
N 秒输出。出问题时分辨「`crates/ssh` 自身 bug vs 上层缝合 bug」的最近现场。

**验证状态**:
- `cargo fmt/clippy/test --workspace` 全绿(含 13 个新 profiles 单测);
- 前端 `tsc + vitest`(152 测试)+ `vite build`(67 KB,gzip 24 KB)全绿;
- 新增 `web/tests/profiles.spec.ts`(5 测试,覆盖 fetch 失败降级到 `[]` 的全部路径)。
- 端到端真连 SSH 远端没有自动化测试 —— 网络依赖,Phase 5 后续加 `russh-server`
  in-process 集成测试(已记 TODO)。手动验证:`~/.perga/hosts.toml` 配一台 host,
  `ssh-add` 加好私钥,`perga-ssh-probe` 或前端设置面板 Connect 跑通即可。

**架构图变化**:

```
   crates/transport (新)
     ├─ TransportCommand / TransportEvent / TerminalSize / ExitStatus
     └─ trait Transport { command_tx(), event_rx() }
              ▲                       ▲
              │                       │
      ┌───────┴─────┐          ┌──────┴──────┐
      │ crates/pty  │          │ crates/ssh  │ (新)
      └─────────────┘          └─────────────┘
              ▲                       ▲
              └───────────┬───────────┘
                          │
                terminal-session(Box<dyn Transport>)
                          ▲
                          │
                perga-server(/api/hosts、?profile=<id> 分流)
                          ▲
                          │
                web 前端(设置面板"远程主机"section)
```

### 已完成 — Phase 5.5:CRUD UI + password auth(2026-05-24)

Phase 5 v1 上线后用户反馈「我不想编 toml」+「平板上不方便」,把 v1 范围里
裁掉的两件事(profile inline 编辑 UI + password auth)提前合到 Phase 5 做完。
落地后用户**完全不接触 hosts.toml** —— 文件成了纯持久化实现细节,UI 是
唯一入口。

- **`crates/ssh`**:`Auth` 加 `Password { password: String }` 变体,
  `session::authenticate_via_password` 走 `Handle::authenticate_password`。
  服务端不允许 password auth 时错误信息提示「server may not allow password auth」。
- **`crates/perga-server::profiles`**:
  - `AuthSpec` 加 `Password { password }` 变体,`hosts.toml` 同形态读写。
  - `HostProfileSummary` 加 `auth_kind: &'static str`(`"agent"` / `"password"`),
    **不返密码明文** —— 密码只在 POST/PUT body 单向流入。
  - 新 `create_profile / update_profile / delete_profile`(+ `_at` 测试隔离版本)。
  - `save_profiles_atomic`:同 `pty::shell_inject::write_atomic` 模式 —— 临时
    文件 + Unix `chmod 0600` + `rename`。配套字段校验(空 user / host / 0 port
    / 空密码且 auth=password 全挡)。
  - `ProfileError` 加 `NotFound` / `Conflict` / `Validation` 变体,直接映射
    到 HTTP 404 / 409 / 422。
- **`crates/perga-server::http`**:`POST /api/hosts`、`PUT /api/hosts/:id`、
  `DELETE /api/hosts/:id`。错误类别映射到标准 HTTP status,body 是 plain text。
- **前端 `state/profiles.ts`**:加 `createProfile / updateProfile / deleteProfile`
  fetch wrappers + `ProfileApiError`(carries status + body)。写路径**不**做
  failure 降级 —— 用户主动操作,静默失败比抛错更糟。
- **前端 `ui/settings_panel.tsx`**:远程主机 section 改成完整 CRUD:
  - 顶部 `+ 添加主机` 按钮,点开 inline form
  - 每行 `编辑 / 删除 / Connect` 按钮(任一行编辑时其他按钮 disabled)
  - 表单字段:**名称 / 用户名 / 主机 / 端口(默认 22)/ 密码**
  - 密码留空 = `agent`,填了 = `password`(form auth dispatch 自动);hint 文
    案告诉用户密码"存于本机 Perga 数据目录,文件权限 0600"
  - 编辑时密码字段是空白(GET 不返密码),用户不填保持 agent,填了 = 替换密码
  - 删除走 `confirm()` dialog,失败 → inline error,不打断已加载列表

**v1.5 范围裁剪**(仍记 `TODO.md`):
- key_file / keyboard_interactive auth(2FA / OTP / 导入私钥);
- 移动端 always-SSH UX(平板 `+` 默认走 SSH,Phase 6 mobile 阶段做);
- OS keychain 集成(Phase 6 Tauri 桌面 / 移动一起评估)。

**为什么明文密码存盘可以接受**:
- 桌面 `~/.perga/hosts.toml` `0600`:同主机其他用户读不到;同用户身份其他
  进程理论上能读,但风险等价于 `~/.ssh/id_rsa` 明文存私钥(用户已经习惯)。
- Tauri 平板 sandbox:每个 app 一个独立 UID(Android)/ App Container(iOS),
  **OS 强制隔离**,别的 app 进程根本访问不到 ── 比桌面 0600 强。
- Phase 6 真上 Tauri 时把 `default_profiles_path()` 一行换成
  `app.path().app_data_dir()`,**其他代码完全不动**。

**测试**:
- 后端新增 11 个 profile 单测(parse / CRUD / 校验 / 0600 perm / password
  roundtrip)。`cargo test -p perga-server` 25 个全过。
- 前端新增 5 个 profiles CRUD spec(POST / PUT / DELETE wrappers + error
  handling)。`vitest` 157 个全过。
- 端到端真连 SSH 远端仍靠手动 `perga-ssh-probe` / 浏览器设置面板 Connect 验证。

### 已完成 — Phase 5.5 review 修复(2026-05-24)

Phase 5.5 实机过后过了一轮 GPT review,三条都 confirmed 是真 bug + 修了:

**1. SSH `Drop` 等满 3s budget(`crates/ssh/src/session.rs`)**
bridge 线程阻塞在 `command_rx.recv()`,Drop 时光发 shuttle shutdown 不能唤醒
它 —— engine 线程仍持有 `command_tx` 的 clone,bridge 干等到 join 超时
detach。修法:bridge 加专用 `bridge_shutdown_tx` 通道,内部用
`crossbeam_channel::select!` 兼听 command_rx + shutdown_rx,Drop 直接发
shutdown 信号。把 bridge 抽到独立 `run_bridge_loop` 函数后,加两个单测:
- `bridge_loop_exits_on_shutdown_even_with_live_sender`:故意保留 sender
  不 drop,验证 shutdown 信号 < 200ms 拉起退出(旧实现这里会 hang 到 3s)。
- `bridge_loop_exits_when_all_senders_drop`:反向兜底。

**2. SSH 连接失败前端看不到原因(`terminal-protocol` + `perga-server` + 前端)**
原实现把 backend spawn 失败折成 HTTP 4xx/5xx,但浏览器 WebSocket API 不把
HTTP body 传给 JS,前端 `onclose` 只看到 generic close → SSH tab 停在空白
fallback,用户不知道是 profile 不存在 / auth fail / fingerprint 不匹配。
- 协议层加 `ProtocolEvent::SessionError { seq, reason }` 变体 +
  `ProtocolEncoder::encode_session_error`。
- server `ws_handler` 改:**只有 size 越界**(纯语法)走 HTTP 400;其他
  backend spawn 失败移到 `on_upgrade(...)` 回调内,失败时发 `SessionError`
  事件再 `socket.close()`。WS upgrade 一定发生,前端通过事件路径拿到错误。
- 前端 `protocol.ts` 加 `session_error` union variant、`session.ts` 加
  `sessionError: string | null` 字段、`session_store.ts` reducer 处理(同时
  置 `exited=true` + `sessionError=reason`)、`pane_leaf.tsx` 顶部加
  `<SessionErrorBanner>` 红框显示原因。
- 加 2 个 reducer 单测覆盖 `session_error` 写入 store + 正常路径下
  `sessionError` 保持 null。

**3. `/api/hosts` 500 被折成空数组掩盖配置 bug(`web/src/state/profiles.ts`)**
原实现 GET 错误 → console.warn + 返 `[]`,UI 显示"还没配主机"。把
`hosts.toml` 解析错误(用户改坏 toml)和"没配主机"混在一起,误导用户。
修法:`fetchProfiles` 改返 discriminated union
`{ kind: "ok", profiles } | { kind: "error", message }`,settings 面板加
`loadError` 派生,失败时显红框 +「通常是 hosts.toml 解析错」提示。
更新 5 个测试断言,加 1 个新 case(空数组 = 没配,**不是** error)。

**验证**:
- `cargo test --workspace` 160 passed(+2 SSH bridge 单测 + 多处
  非穷尽 match 修复 `terminal-session::tests` / `terminal-protocol::tests/marks`)
- 前端 `vitest` 160 passed(+2 session_error reducer + 5 个 fetchProfiles
  断言重写)
- `pnpm build`:75 KB / gzip 26.7 KB

### 已完成 — vite proxy `/api`(2026-05-24)

CRUD UI 上线后第一次实测炸了一个 SyntaxError:
`Unexpected token '<', "<!doctype "... is not valid JSON`。根因:
`vite.config.ts` 只代理了 `/ws`,新加的 `/api/hosts` 走了 SPA fallback 返
`index.html`,前端拿 HTML 当 JSON 解析。修法:proxy 加一条 `"/api"` →
`http://127.0.0.1:7777`。已留注释:**新增 REST 端点时记得同步加 proxy**。

---

### 已完成 — Phase 6:Tauri 打包(Linux 桌面 + 移动端 UX 架构准备,2026-05-24)

**驱动**:产品要支持 iPad / Android 远程 SSH(§1.3),那两个平台**不允许绑端口、
不允许 spawn `ssh` 子进程**,所以 `axum + WebSocket + 7777` 在移动端起不来,
必须换成 Tauri IPC(`invoke` + `event`)。Tauri 2 是唯一能同时覆盖
Linux/Win/macOS/iOS/Android 的方案。

**用户对齐的决策**:

1. **不双形态**:打包后只走 Tauri IPC;`perga-server` 留作 **dev-only adapter**
   (浏览器迭代 + devtools)。
2. **Linux 桌面优先**:v1 = Linux Tauri binary。iOS / Android build 留 Phase 6.5。
3. **移动端 always-SSH UX 是硬约束**:架构 / 前端代码 v1 必须容纳。`?platform=mobile`
   URL flag 在浏览器里就能验证 mobile 路径。
4. **`+` 按钮**:桌面 → 本地 shell;移动 → 每次弹 profile picker。
5. **Split**:移动端不特殊处理(平板带键盘 = 桌面同款行为)。

**三层架构**(抽象出 `crates/perga-core`):

```
            ┌─────────────────────────┐
            │   crates/perga-core (新) │   ← profiles / wire /
            │                         │     session_factory
            │   无 axum / 无 tauri     │     共享于两端
            └────────────┬────────────┘
                         │
       ┌─────────────────┴────────────────┐
       ▼                                  ▼
 ┌──────────────┐                ┌──────────────────────┐
 │ perga-server │                │ crates/perga-tauri (新)│
 │ (dev only)   │                │  Tauri 2 app,         │
 │ axum/WS/HTTP │                │  9 commands,          │
 │ thin wrapper │                │  SessionRegistry,     │
 └──────────────┘                │  app_data_dir 路径    │
       ▲                         └──────────┬────────────┘
       │ ws/http                            │ invoke / event
       ▼                                    ▼
   ┌───────────────────────────────────────────┐
   │  web 前端                                  │
   │  Transport interface (ws / tauri 两份实现)  │
   │  ProfileApi interface (ws / tauri 两份实现) │
   │  isTauri() / detectPlatform()              │
   └───────────────────────────────────────────┘
```

`TerminalSession` / `Transport` trait / `crates/{pty, ssh, terminal-*}` **0 改动** —
前面几个 phase 围绕「后端是事实来源 + Transport trait 化」打的底子直接套用。

**关键代码改动**:

- **`crates/perga-core` 新建**:从 perga-server 搬来 `profiles.rs` + `wire.rs`;
  新增 `session_factory.rs` 暴露 `open_local(size, cwd) -> Result<TerminalSession, OpenError>`
  与 `open_ssh(profile, size, known_hosts_path) -> Result<TerminalSession, OpenError>`。
  `to_ssh_config` 签名加上 `known_hosts_path: Option<PathBuf>` 参数(Tauri 注入
  `app_data_dir`,server 传 `None` 走 `~/.ssh/known_hosts`)。
- **`perga-server` 变薄**:lib.rs 改 `pub use perga_core::wire::ClientMessage`;
  `ws.rs` / `http.rs` 全部 thin 化为 axum extractor + 调用 perga-core 函数。
  `bridge.rs` 留下(axum WS 专属 sync↔async 缝合)。
- **`crates/perga-tauri` 新建**:Tauri 2 应用,9 个 commands(profile CRUD 4 个、
  session 3 个、`get_platform_info`)。`SessionRegistry: Mutex<HashMap<SessionId,
  Arc<SessionEntry>>>` 持有 session;每个 session 一条 OS 线程 emit
  `session_event:<uuid>` 到 webview。`paths::resolve(app)` 把
  `~/.local/share/io.perga.app/{hosts.toml, known_hosts}` 注入 `AppState`。
- **前端 transport 抽象**:`web/src/net/transport.ts` 定 `Transport` 接口,
  `ws.ts` / `tauri.ts` 两份实现;`net/index.ts` 按 `isTauri()` 二选一。Tauri 实现
  用动态 import + queue,setup 期间 send 暂存,session_open 完成后 flush。
- **前端 ProfileApi 抽象**:`state/profiles.ts` 重做为 `httpProfileApi` /
  `tauriProfileApi` 两份。Tauri 实现把 command 字符串错误前缀 (`not_found:` /
  `conflict:` / `validation:` / `io:`)映射回 HTTP 等价 status,UI 共用错误展示。
- **平台检测**:`web/src/util/platform.ts` 暴露 `isTauri()` 与 `detectPlatform()`。
  `detectPlatform` 三层 fallback:URL flag → Tauri command `get_platform_info` →
  默认 desktop。
- **移动端 UX**:`web/src/ui/profile_picker.tsx` 触控友好 modal,大块 tap target;
  `forceSetup=true` 模式自动切到 HostForm(用户必须先添加首台主机)。`tab_bar.tsx`
  + 按钮支持 `onPlusOverride` —— App 在 mobile 时传入「打开 picker」。
  `HostForm` 从 settings_panel 抽到 `host_form.tsx` 复用。`app.tsx` 顶层
  `createResource(detectPlatform)`,mobile 首启动自动弹 picker。

**Tauri 配置**:

- `crates/perga-tauri/tauri.conf.json` —— window 1280×800,bundle deb +
  AppImage,beforeDevCommand `pnpm --dir ../../web dev`,frontendDist
  `../../web/dist`,devUrl `http://localhost:5173`。
- `capabilities/default.json` —— `core:default`(custom commands 在 Tauri 2 不
  需要显式 allow)。
- `icons/{32x32,128x128,128x128@2x,icon}.png` —— 1×1 透明 RGBA 占位,真稿留
  Phase 6.5。

**验证(本轮跑过的)**:

- `cargo build -p perga-tauri` ✓(产物 `target/debug/perga`,266 MB debug build)
- `cargo tauri info` ✓ 识别 frontendDist / devUrl / webkit2gtk
- `cargo test --workspace` ✓ 全部通过
- `cargo clippy --workspace --all-targets -- -D warnings` ✓ 0 warning
- `pnpm exec tsc --noEmit` ✓
- `pnpm exec vitest run` ✓ 165 / 165(新增 5 个 platform 探测测试)
- `pnpm build` ✓,vite 自动把 `@tauri-apps/api/core` + `event` 拆成独立 chunk

**Phase 6 v1 不做(进 TODO.md)**:Android / iOS bundle(签名 + SDK)、PTY cfg
gate for mobile、`workspace` 在 mobile 形态下不预创建 default tab、icon 完整稿、
代码签名 / auto-updater、Windows / macOS 桌面实测。

---

**下一步 = Phase 6.5 mobile build,或 终端级选择复制增强**。Phase 6.5 需要先
解开 `workspace` "≥1 tab" invariant + `crates/pty` 加 `#[cfg]` gate,然后
`cargo tauri android init` / `ios init`,准备签名。

---

## 6. Phase 2 详细规划 — Tabs + Panes

> ✅ **已实现(本轮)**。以下是当初的规划;下方键盘表、未决问题已就地修订为实际
> 落地结果,实现细节见 §4。

**目标**:多 session 通过顶部 tabs 切换;每个 tab 内可水平 / 垂直分屏(panes,二叉嵌套树);每个 pane = 一条独立 WS = 一个独立 session。

**后端**:**无新改动**。每条 WS 已经独立 spawn session,server 不需要 registry。

**前端新文件**:

| 文件 | 内容 |
|---|---|
| `web/src/state/workspace.ts` | `Workspace = { tabs: Tab[], activeTab: string }`,`Tab = { id, title, root: PaneTree, focusedLeaf: string }` |
| `web/src/state/pane_tree.ts` | `PaneTree = { kind: 'leaf', id, sessionId } \| { kind: 'split', axis, ratio, a, b }`,纯函数操作:`splitFocused` / `closeFocused` / `focusNeighbor` / `setRatio` |
| `web/src/ui/tab_bar.tsx` | 顶部 tab 栏(active 高亮,关闭 ×,`+` 新建) |
| `web/src/ui/pane_tree_view.tsx` | 递归渲染 PaneTree → flex 布局,6px gutter |
| `web/src/ui/pane_leaf.tsx` | 单个 leaf 渲染:`<GridCanvas>` + 自己的 SessionSocket(从 `ui/app.tsx` 抽出来) |
| `web/tests/pane_tree.spec.ts` | 纯函数树操作单测 |

**前端修改**:`web/src/ui/app.tsx` 从单 session 改成 Workspace 多 tab,顶部 `<TabBar>` + 主区 `<PaneTreeView>`。原 `app.tsx` 里的 SessionSocket / keyboard / paste 绑定全部下沉到 `pane_leaf.tsx`。**Focus 路由**:keyboard 事件路由到当前 focused leaf。

**键盘约定**(实际实现 —— 全部收在 `Ctrl+Shift+` 一族:纯 Web 下浏览器 chrome 会
抢占 `Ctrl+T/W/Tab/数字` 且页面无法 preventDefault,进 Tauri 后可重排):

| 快捷键 | 行为 |
|---|---|
| `Ctrl+Shift+D` | 垂直 split(竖切) |
| `Ctrl+Shift+E` | 水平 split(横切) |
| `Ctrl+Shift+X` | 关闭当前 pane |
| `Ctrl+Shift+Enter` | 新 tab |
| `Ctrl+Shift+1..9` | 切到第 N tab |
| `Ctrl+Shift+→` | 下一 tab |
| `Alt+方向键` | 焦点移到相邻 pane(空间上 next) |

**Drag-resize gutter**:每个 split 在边界画 6px gutter,`pointerdown` → 全局 `pointermove`(节流 / RAF)→ 改 `ratio` → `pointerup` 提交。

**未决问题**:

- 关闭最后一个 pane → 关闭整个 tab(多 tab 时);若已是单 tab 单 pane,则 respawn
  一个全新 pane。
- 关闭最后一个 tab → respawn 一个全新 tab。两者统一为**无空态不变量**(决策 B):
  `PaneTree` 永不为空、workspace 永不无 tab,消除「空 tab」这个特殊情况。
- Tab title:Phase 2 用 server 端 OSC 0/2 标题(已有,在 `ProtocolEvent.title`)。OSC 0/2 没设置时落 fallback "shell"。

**验收**:

- 手动:可以 4 pane 同时跑独立 shell,Ctrl+Shift+D / W、Alt+Arrow、tab 切换都能用。
- Playwright e2e:开两 tab,split 一个,关掉 → server 端 PTY 子进程数 = 剩余 pane 数。

---

## 7. Phase 3 详细规划 — OSC 133 + Warp 风格命令块

> ⚠️ **命令块系统已于 2026-05-23 整体移除**(见 §5 末节)。本节是历史规划,
> 仅作背景保留。OSC 133 shell 集成本身保留,改用途为「标记失败命令」。
>
> ✅ **已实现(2026-05-21)**。下方是当初规划 + 探测修订;落地结果以代码
> 和 §5 / §12 / §13 为准。
>
> 🔬 **探测修订(2026-05-21)**:OSC 133 落地前先跑了一次纯探测
> (`crates/perga-cli/src/bin/probe_osc133.rs` + `scripts/perga-bash.sh`),
> 起真实 bash 实测 mark 行号语义,结论见 §13。两点改了本节原规划:
> ① `feed()` 双喂必须在 OSC 边界**交错**推进两个状态机,不能整块喂完再喂;
> ② block 内容由**后端**在 CommandEnd 组装下发,前端不再切自己的视口
> —— 视口行号会因滚动失效。下方已就地修订。

**目标**:完成的命令收成可折叠 DOM block(可选中复制),当前正在输入 / 执行的命令仍是 inline Canvas grid 终端体验。

**OSC 133 协议**(shell integration 标准):

| 序列 | 含义 |
|---|---|
| `\e]133;A\e\\` | Prompt start |
| `\e]133;B\e\\` | Prompt end / command start(用户已经回车) |
| `\e]133;C\e\\` | Command execution start(命令开始运行) |
| `\e]133;D;<exit>\e\\` | Command finished,exit code |

需要用户在 shell 配置里 source 我们的 setup 脚本才会发这些序列。**不强制**:没 source 时降级到纯 Grid 模式,UX 平滑过渡。

**后端新文件**:

| 文件 | 内容 |
|---|---|
| `crates/terminal-engine/src/shell_integration.rs` | `ShellIntegration` 含一个 `vte::Parser` 旁路 + `Osc133Perform`,只 implement `osc_dispatch`。`ShellMark` 枚举:`PromptStart` / `CommandStart` / `CommandExecuted` / `CommandFinished { exit }` |
| `crates/terminal-engine/tests/osc133.rs` | 喂含 OSC 133 的字节,断言 marks 序列 |
| `crates/terminal-protocol/tests/marks.rs` | 三个新 ProtocolEvent 变体 round-trip serde |
| `scripts/perga-bash.sh` | `PS0` 钩 C、`PROMPT_COMMAND` 钩 D、`PS1` 钩 A/B(✅ 已建) |
| `scripts/perga-zsh.sh` | `precmd` / `preexec` 钩 OSC 133 |

**后端修改**:

- `crates/terminal-engine/src/lib.rs`:`mod shell_integration` + re-export `ShellMark`。
- `crates/terminal-engine/src/engine.rs`:`TerminalEngine` 持 `ShellIntegration`,`feed()` 内部**双喂**(alacritty + 旁路 vte)。⚠ 双喂必须在 OSC 边界**交错**推进两个状态机 —— 整块喂完 alacritty 再喂 vte,会让 mark 的光标采样落到 chunk 末尾、行号全错(探测已证实)。`ShellMark` 在旁路 parser 命中那一刻立即从 alacritty 抓**绝对行号**。新增 `drain_marks() -> Vec<ShellMark>`(对称 `drain_pending_writes`)。
- `crates/terminal-protocol/src/event.rs`:`ProtocolEvent` 加 `PromptStart { seq, line }` / `CommandStart { seq, line }` / `CommandEnd { seq, exit: Option<i32>, rows }`。`line` 是 alacritty 的**绝对行号**(不随滚动失效),不是视口行;`CommandEnd.rows` 直接携带后端组装好的 block 行段(复用既有 RLE `RowEntry` 编码),前端不再自己切视口。
- `crates/terminal-protocol/src/encoder.rs`:加 `encode_mark(ShellMark) -> ProtocolEvent`;收到 `D` mark 时,后端从 grid(含 scrollback)按绝对行取 `[CommandStart.line, CommandEnd.line)` 组装 `CommandEnd.rows`。seq 沿用单调计数器。
- `crates/terminal-session/src/event_loop.rs::handle_pty_event`:`engine.feed()` 后**先 drain marks emit,再 emit_frame**。绝对行号本身不怕 Patch 乱序,但 emit 顺序仍要保证 —— `CommandEnd`(带 block 行段)必须在对应 Patch 之前到,前端才能在正确时机把活动区收成块。

**前端新文件**:

| 文件 | 内容 |
|---|---|
| `web/src/state/blocks.ts` | `CommandBlock = { id, exit, rows: RowEntry[][], capturedAt, startedAt }`,reducer 在 `CommandEnd` 时直接收下后端组装好的 `rows`,**不**自己切视口 —— 视口只有一屏,超一屏的命令切不全(探测已证实) |
| `web/src/render/block_list.tsx` | `<For>` 虚拟列表 DOM 渲染,foldable / 可选中复制 |
| `web/src/render/block_one.tsx` | 单个 block 的 DOM:命令行 header + 输出 body + exit 状态徽章 |

**前端修改**:

- `web/src/state/protocol.ts`:TS 加三个新 event variant。
- `web/src/state/session.ts`:`SessionState` 加 `marks` 字段(临时存当前进行中的 PromptStart / CommandStart row)+ `blocks: CommandBlock[]`(append-only),reducer 处理新事件。
- `web/src/state/session_store.ts`:`produce` 路径加 mark 处理。
- `web/src/ui/pane_leaf.tsx`(Phase 2 出的那个)→ 布局改成「上面 `<BlockList>` + 下面 `<GridCanvas>` 始终底部」。`alt_screen` 时挂起 BlockList 让 Canvas 占满。
- `README.md`:加 "Shell Setup" 段:`source scripts/perga-zsh.sh`(用户 opt-in)。

**设计要点**(已经定,不要反复纠结):

- OSC 133 用**旁路 `vte::Parser`**,**不**去 strip 原字节(alacritty 0.25 静默丢未注册 OSC,引擎状态零污染 —— 探测已确认)。
- OSC 133 双喂**必须在 OSC 边界交错**推进两个状态机,不能整块喂完 alacritty 再喂 vte,否则 mark 的光标采样落在 chunk 末尾(探测已证实)。
- mark 携带 alacritty **绝对行号**(不是视口行);block 内容由**后端**在 CommandEnd 从 grid(含 scrollback)组装下发。视口行号会因滚动失效,前端切不出超一屏的块。
- 协议事件序:**marks 在 patch 之前** emit。
- Block **不 reflow**:resize 时 block 保持原宽,过宽横向滚动。「数据模型优先 + 不为未来写代码」(`CLAUDE.md`)。
- Block ↔ Canvas **跨边界选择**:Phase 3 **不做**,延后到 Phase 7+。
- alt_screen 模式时 BlockList 暂停(vim / tmux 全屏占 Canvas)。
- 没 source shell 脚本的用户:看不到 block,但 Grid 一切正常工作 ── 降级路径就是 Phase 1 状态。

**验收**:

- `websocat` 能看到 `prompt_start` / `command_start` / `command_end` JSON 帧。
- 浏览器:跑 `echo hi` 看到 DOM block 出现,光标行依旧 inline 在 Canvas。
- 跑 `seq 1 100`(输出远超一屏)→ block 完整收下 100 行,无丢行。
- vim 进 alt screen → BlockList 挂起;`:q` 退出 → 恢复。

---

## 8. Phase 4 — Zoom + 主题 + 命令块重做 + 视觉抛光

> ✅ **已实现(2026-05-22)**。落地结果以代码和 §5 为准。下面是最终定稿要点。
>
> ⚠️ 其中「命令块 Warp 风格重做 / 右键菜单 / `blockStyle` 设置」已于
> 2026-05-23 随命令块系统一并移除(见 §5 末节);主题 / 缩放 / 设置面板 /
> tab 自动隐藏 / 非焦点变暗保留。

**目标**:自定义缩放;深 / 浅双主题;命令块 Warp 风格重做;视觉抛光。

**新文件**:

| 文件 | 内容 |
|---|---|
| `web/src/render/theme.ts` | `ThemeId` / `THEMES`(dark/light)/ `applyTheme` / `paletteForTheme` |
| `web/src/state/settings.ts` | `Settings = { zoomPercent, themeId, blockStyle }`,localStorage 持久化 |
| `web/src/state/settings_context.ts` | `SettingsContext` + `useSettings` |
| `web/src/input/zoom_shortcuts.ts` | `matchZoomShortcut`(Ctrl+Shift+=/−/0) |
| `web/src/ui/modal.tsx` | 通用居中模态浮层 |
| `web/src/ui/settings_panel.tsx` | 设置面板:zoom / 主题 / 命令块风格 |
| `web/src/ui/context_menu.tsx` | 通用右键菜单 |

**关键决策**:

- **颜色二分**:`colorToCss` 在 Canvas 热循环里每帧每 run 调用,`fillStyle`
  不接受 `var()`。故拆 `colorToDomCss`(named/0..15 → `var(--term-…)`,DOM 块
  用,切主题零重渲染)与 `colorToCanvasCss`(具体色,Canvas 用,每帧解析一次
  主题调色板传入)。
- **缩放快捷键用 `Ctrl+Shift+=/−/0`**:裸 `Ctrl+=/-/0` 是浏览器页面缩放键,
  Chrome 不让 `preventDefault`。进 Tauri 后可加裸键。
- **zoom 联动 resize**:zoom 改 cell 尺寸但容器不变,`ResizeObserver` 不 fire
  → `pane_leaf` 在 zoom effect 里重测 metrics、重建 watcher、主动补发 resize。
- **命令块三风格**(stripe / tinted / minimal)做成设置项,由用户选;三者只差
  外层卡片 chrome,行渲染共享。
- **tab 栏自动隐藏**:`position:absolute` 浮层,单 tab 隐藏 / hover 显示。单
  tab 的 hover 显隐**不改 `paneArea` padding** → 不触发 resize。
- 设置走 `SettingsContext` 跨递归 pane 树下发,不逐层 prop-drilling。

**已知限制**:命令块相关限制已随 2026-05-23 的命令块移除失效。当前复制限制
转为「活动区 Canvas 无法原生选中」,详见 `TODO.md` 的终端选择复制项。

---

## 9. Phase 5 详细规划 — SSH (russh) + host profile

> ✅ **已实现(2026-05-24)**。落地结果见 §5 末节 + 代码。下面是当初规划,
> 实际产物在以下几处与原规划不同,以代码为准:
>
> - **抽象选 trait,不选 enum**:`terminal-session::TerminalSession` 内部
>   `Box<dyn Transport>`,trait 提到了**新的** `crates/transport` crate 而不是
>   `terminal-session::session` 模块里(避免 pty / ssh 反向依赖 terminal-session)。
> - **`SshEvent` / `SshCommand` 不单独存在**:全 workspace 统一到
>   `transport::TransportEvent` / `transport::TransportCommand`。
> - **profile store v1 只读**:`POST/PUT/DELETE /api/hosts` 没做;`GET /api/hosts`
>   返回不含 auth 的摘要。CRUD 入 `TODO.md`。
> - **Auth 只实现 `Agent`**(schema 留 tagged enum 形态扩展位)。
> - **Jump host / agent forwarding / SSH OSC 133 全不做**(入 `TODO.md`)。
> - **前端走设置面板 side-door,不弹 modal 选 local/SSH**:`+` / `Ctrl+Shift+Enter`
>   行为 0 变化,见 §5 末节 UX 决策。
> - **未决问题更新**(见原 §9 末尾):known_hosts 用系统 `~/.ssh/known_hosts`
>   做静默 TOFU;Password 完全不做;Agent forwarding 不做;Jump host 不做;
>   SSH 断网时 shuttle_loop 发 `Exited` 让前端走已有断连路径。

**目标**:用户能在前端"添加主机 → 一键连接",底层走 russh embedded(不 spawn `ssh` 子进程)。

**为什么必须 russh**:iPad / Android(Phase 6 移动 Tauri)沙箱不允许 spawn 子进程;桌面也跟着 russh 统一一套代码。

**新 crate**:`crates/ssh`(与 `crates/pty` 平行):

| 文件 | 内容 |
|---|---|
| `src/lib.rs` | re-exports |
| `src/session.rs` | `SshSession` 类型,与 `PtySession` 接口对称(`command_tx`, `event_rx`, Drop 清理) |
| `src/config.rs` | `SshConfig { host, user, port, auth, jump_host: Option<Box<SshConfig>> }` |
| `src/auth.rs` | `SshAuth::{KeyFile, Password, Agent}`;`Agent` 透传系统 ssh-agent(需查 russh 支持) |
| `src/event.rs` | `SshEvent`(`Output` / `Exited` / `Error` 与 `PtyEvent` 一致) |
| `src/runtime.rs` | russh 是 async,这里起一个 mini tokio runtime 跑 SSH session;对上层暴露同步 channel 与 `PtySession` 完全兼容 |
| `tests/integration.rs` | 用本机 sshd 或 mock 测试 |

**抽象**:`crates/terminal-session/src/session.rs` 抽出 `trait ByteSource` 或者 `enum SessionBackend { Local(PtySession), Ssh(SshSession) }`,`TerminalSession` 用其中之一。两个 spawn 入口:`TerminalSession::spawn_local(PtyConfig)` / `TerminalSession::spawn_ssh(SshConfig)`。

**后端新文件 / 修改**:

| 文件 | 内容 |
|---|---|
| `crates/perga-server/src/profiles.rs`(新) | host profile store:`~/.perga/hosts.toml` 读写,profile CRUD |
| `crates/perga-server/src/http.rs`(新) | REST 端点:`GET / POST / PUT / DELETE /profiles/:id` |
| `crates/perga-server/src/ws.rs`(改) | query 加 `?profile=<id>`,handler 走 SSH 路径而非 local PTY |
| `crates/perga-server/src/lib.rs`(改) | router 加 `/profiles` 路径 |

**前端新文件**:

| 文件 | 内容 |
|---|---|
| `web/src/state/profiles.ts` | host profile 类型 + REST fetch wrapper |
| `web/src/ui/profile_manager.tsx` | 管理界面:增 / 删 / 改 host,测试连接按钮 |
| `web/src/ui/connect_dialog.tsx` | 新开 pane 时弹出 "本地 shell vs. SSH host" 选择,SSH 选哪个 profile |

**未决问题**(Phase 5 开始时拍):

- **known_hosts**:用系统的 `~/.ssh/known_hosts` 还是项目自己存?第一版用系统的,省事。
- **Password prompt**:支不支持密码 auth?第一版**只支持 key + agent**,password 留下一刀。
- **Agent forwarding**:能否透传系统 ssh-agent 到远程?需要查 russh 文档。
- **Jump host (ProxyJump)**:russh 支持,前端 UI 复杂。**第一版不做**。
- **session 在 SSH 断网时怎么处理**:服务端 SshSession 收到网络错误时发 `Exited`,前端按已有断连路径处理。

---

## 10. Phase 6 详细规划 — Tauri 打包

> ✅ **Linux 桌面 v1 已实现(2026-05-24)**。落地结果与本节当初规划的差异:
>
> - **新 crate 名定为 `crates/perga-tauri`**(不是 perga-app);并新增
>   `crates/perga-core` 作为协议无关共享层(server + tauri 共用)。
> - **Transport 双实现** 落实:`createWsTransport` / `createTauriTransport` +
>   `transportFactory` 按 `isTauri()` 二选一,业务代码完全不感知。
> - **移动端 UX 架构提前接入**(用户拍板的硬约束):前端 `+` 按钮分支、
>   profile picker、`detectPlatform()` 三层 fallback、`?platform=mobile` URL
>   flag dev 验证 —— 都在 v1 完成。**实际 mobile bundle 留 Phase 6.5**。
> - **dev 浏览器形态保留 perga-server**(用户拍板:不双形态 ≠ 不要 dev);
>   打包后 perga-server 不进 binary。
> - **不做**:Tauri updater / 代码签名 / icon 完整稿 / Windows / macOS bundle 验证(全部进 TODO.md)。
>
> 详细落地见 §5 `Phase 6:Tauri 打包` 段。

下面保留当初的规划文字,供历史对照:

**目标**:把 Web 前端打包成桌面 app(Linux 优先 → Mac → Windows),然后 iPad / Android。

**新 crate**:`crates/perga-app`(Tauri 2 标准结构)。

**关键改动**:`web/src/net/ws.ts` 抽象出 `Transport` 接口,**两个实现**:

- `WebSocketTransport`:现有逻辑。
- `TauriIpcTransport`:用 `@tauri-apps/api` 的 `invoke` + `listen` 替代 WS 帧。Tauri 后端注册 `#[tauri::command] async fn send(...)` 和 emit event。

启动时 detect `window.__TAURI__` 存在与否,自动选 transport。**前端业务代码完全不感知**这层差异。

**Tauri 后端**:

| 文件 | 内容 |
|---|---|
| `crates/perga-app/src-tauri/src/main.rs` | Tauri 入口,注册 commands,启 PTY/engine/session 同一套核心(直接 import 现有 5 个 crate) |
| `crates/perga-app/src-tauri/tauri.conf.json` | bundle 配置 |

**移动版**(iPad / Android):**Tauri 2 mobile 当前状态决定**。如果 mobile 还不稳定,这部分延期。需要查最新 Tauri 2 mobile 文档。

平板专属问题:

- 触屏 split / 关闭手势。
- 虚拟键盘弹起时不能挡终端。
- 横屏 ↔ 竖屏切换 → resize。
- 键盘特殊键(Esc / Ctrl)在 iOS 软键盘上没有 → 需要 UI 提供。

---

## 11. Phase 7+ — 长尾功能 / 待回答的问题

排序大致按用户感知优先级,但不强制顺序:

| 项 | 描述 | 触发条件 |
|---|---|---|
| Terminal selection/copy | 当前活动区已是 DOM grid,浏览器原生复制可覆盖可见 DOM 文本,但还没有终端 selection model。要做前端 selection model:history+grid 统一坐标、pointer drag 选区、overlay 高亮、长范围复制;无选区时 Ctrl+C 继续发 SIGINT | 用户要复制长范围历史 / 需要终端语义复制 |
| Mouse reporting | 后端 wire/session/input encoder 已实现,前端只有 TS wire 类型,还没绑 pointer/wheel 发送。复制选择先默认接管鼠标;后续接 mouse reporting 后,`mouse_reporting !== off` 时默认把鼠标交给 TUI,`Shift/Alt+Drag` 强制前端选择 | 用户要 tmux/vim/less/htop/lazygit 鼠标操作 |
| IME 输入 | `compositionstart` / `compositionend`,合成完整字符串后整段发 paste 帧 | 用户 CN 输入需要 |
| 主题自定义 | TOML 配置文件,用户可定义 16 色 palette + 字体 | Phase 4 之后 |
| Scrollback 持久化 | session 关闭后保留输出记录 | 用户报告需要 |
| 服务端 session listing | 跨 window 多连接接同一 session | 想做 reconnect 时考虑 |
| WS 重连 | 故意不做(reconnect ≠ 接到原 session)── 但 mobile 网络抖动可能强制要求 | Phase 6 mobile 时评估 |

**横向问题**(任何阶段都可能撞上):

- **用户配置文件**:`~/.config/perga/config.toml`?Phase 4 zoom / theme 落地时定下来。
- **错误恢复**:server 进程崩了所有 session 死。需不需要 supervised mode?Phase 5+ 看实际体验。
- **资源限制**:max sessions per server?max scrollback size?目前 unbounded,Phase 5+ SSH 多用户可能撞。
- **可观测性**:tracing 已铺,需要时 `RUST_LOG=info` 看;真要 prod 部署再加结构化日志输出。
- **CI**:目前只 `cargo test` + `pnpm exec vitest run` 本地跑。需要时加 GitHub Actions。

---

## 12. 设计决策清单(都已和用户对齐)

跨会话需要记住、容易反复纠结的几个决定:

| 议题 | 决定 | 理由 |
|---|---|---|
| Tauri vs 纯 Web | **纯 Web 先做,Tauri 后置** | Tauri 调试摩擦大;真正问题是 transport 形态,Tauri 后期只是把 WebSocket 换 Tauri IPC,前端代码不动 |
| HTTP control plane | **不做**,只 `/ws?rows=&cols=` | Phase 0-4 都没 reconnect / session listing 需求,加了就是死代码 |
| 多 WS 多路复用 | **不做**,一会话一连接 | tabs / panes 是前端概念,每个 leaf 自己开一条 WS,server 无状态 |
| SSH 实现 | **deferred 到 Phase 5+,届时用 russh** | iPad 沙箱不允许 spawn `ssh` 子进程;桌面也跟着 russh 统一一套 |
| 远程访问 auth | **localhost-only bind,部署阶段用 Tailscale** | 不写鉴权代码 |
| 前端框架 | **Solid** | Signals 细粒度更新最契合「dirty-row 高频小 patch」;用户不会前端,Solid API 易学 |
| Grid / scrollback 渲染 | **活动区 DOM grid,历史区虚拟 DOM 行** | 统一 DOM 文本渲染,为复制 / IME / 选区高亮降低后续复杂度;活动区仍保持 cell grid 数据模型与 dirty-row 更新 |
| OSC 133 接入 | **vte 旁路 Parser**,双喂、按 OSC 边界交错 | alacritty 0.25 不暴露 OSC 133,vte 默认丢弃;不 strip(alacritty 静默丢);交错喂才能采到正确 mark 行 |
| bash 集成 `C` 钩子 | **走 `PS0`,不走 `DEBUG` trap** | DEBUG trap 会被 PROMPT_COMMAND 每条命令、PS1 命令替换乱触发,和用户已有的 starship / direnv 冲突;`PS0` 是「命令执行前」的独立钩子,与 PROMPT_COMMAND 无关,干净(bash 4.4+) |
| 协议事件序 | **`command_end` 先于对应 patch** | 前端要先记录失败命令行号,随后 patch / scrolled_rows 把该行滚进 history 时才能落红色失败标记 |
| marks 上不上 wire | **只 `command_end` 上 wire** | prompt / command-start 仍是后端内部状态;前端当前只需要失败命令的 exit + line,回显 settle 不值得单开事件 |
| scrollback wire | **Patch 携带 `scrolled_rows` / `cleared`,不再有 `active_top`** | 命令块移除后 Canvas 画完整 viewport;滚出 viewport 的历史由前端累积,`CSI 3J` 通过 `cleared` 清 history |
| resize / alt-screen 重新基准化 | **`scroll_total` 归零、丢在途 command region** | reflow / alt-screen 会让绝对行号语义失真;代价是跨切换的那条失败标记可能丢失,罕见可接受 |
| WS 重连 | **故意不做** | Reconnect ≠ 接到原 session(WS 一断后端 PTY 就死);重连等同新开会话,UX 上必须明示 |
| 复制 vs TUI 鼠标 | **前端选择系统要尊重 `mouse_reporting`** | 现阶段前端还没发 mouse 帧,可默认拖拽选择;后续 TUI 鼠标接入后,mouse reporting 开启时默认鼠标交给应用,用 `Shift/Alt+Drag` 强制选择文本 |

---

## 13. 已知风险 / 待回答的问题

- **性能天花板**(已回测):前端处理已不是瓶颈 ── dispatch p99 0.4ms、latency enter→settle p99 4.3ms。剩下的是 Web 架构固有的 ~33ms 输入→显示下限(浏览器合成 + 显示器刷新),Wezterm 走 GPU 原生约 8-16ms,改不掉,接受。
- **PTY 子进程清理**:`TerminalSession::Drop` 已经覆盖了 SIGHUP→500ms→SIGKILL 序列,terminal-session 测试覆盖。但 perga-server 在 server 进程异常退出时(panic 非 catch_unwind 路径)是否能保证清掉所有 PTY?需要 Phase 2 加 session registry 时一并审。
- **OSC 133 行号语义**(✅ 已落地):探测确认 mark 的语义行能精确采到
  (A/B 在 prompt 行、C 在输出首行、D 在末输出行下一行),但视口行号会因滚动
  失效。Phase 4R 后命令块已移除,OSC 133 只用于 `command_end { exit,line }`
  失败标记;line 仍靠后端绝对行号。
  探测器 `probe_osc133.rs` 已随 Phase 3 收尾删除,回归由
  `crates/terminal-engine/tests/osc133.rs` 接管。
- **scrollback 饱和**:绝对行号 / `scrolled_rows` 仍靠 alacritty
  `history_size()` 增量累计;scrollback 上限 10000 行,饱和后增量恒 0、历史停止
  增长、失败标记行号也会漂移。详见 `TODO.md`。
---

## 14. 快速命令参考

```bash
# 跑后端(必须 release,否则慢 10×)
cargo run --release -p perga-server

# 跑前端 dev
cd web && pnpm dev
# 浏览器:http://localhost:5173/?perf=1

# 跑所有测试
cargo test --workspace
cd web && pnpm exec vitest run

# 性能基准
cargo run --release -p perga-cli --bin perga-bench
cargo test --release -p perga-server --test rtt -- --nocapture

# SSH 字节通路手动 probe(需 ssh-agent + ssh-add 加好私钥)
cargo run --release -p perga-cli --bin perga-ssh-probe -- --host <host> --user <user>

# 查看可用 host profile(server 跑着的时候)
curl -s localhost:7777/api/hosts | jq

# Lint gate
cargo clippy --workspace --all-targets -- -D warnings
cd web && pnpm exec tsc --noEmit
```

---

## 15. 文件改动统计(Phase 0–6)

| 区域 | 新增文件 | 修改文件 |
|---|---|---|
| Phase 0/1 backend | `crates/perga-server/` 全套 (9 files), `crates/perga-cli/src/bin/bench.rs`, `crates/perga-server/tests/{lifecycle,rtt}.rs` | `Cargo.toml`, `crates/perga-cli/Cargo.toml` |
| Phase 0/1 frontend | `web/{package.json,tsconfig.json,vite.config.ts,index.html,.gitignore}`, `web/src/**/*.{ts,tsx}` (12 files), `web/tests/*.spec.ts` (2 files) | — |
| Phase 2 frontend | `state/{pane_tree,workspace}.ts`, `input/workspace_shortcuts.ts`, `ui/{pane_leaf,pane_tree_view,tab_bar}.tsx`, `tests/{pane_tree,workspace,workspace_shortcuts}.spec.ts` | `ui/app.tsx`, `render/{grid_canvas,metrics}.ts`, `input/resize.ts`, `util/autotest.ts` |
| Phase 3 backend | `terminal-engine/src/shell_integration.rs`, `terminal-engine/tests/osc133.rs`, `terminal-protocol/tests/marks.rs`, `scripts/perga-{bash,zsh}.sh` | `terminal-engine/src/{engine,lib}.rs`, `terminal-protocol/src/{event,encoder,lib}.rs`, `terminal-session/src/{event_loop,session}.rs` |
| Phase 3 frontend | `state/blocks.ts`, `render/{block_one,block_list}.tsx`, `tests/blocks.spec.ts` | `state/{protocol,session,session_store,workspace}.ts`, `render/grid_canvas.tsx`, `ui/pane_leaf.tsx`, `util/autotest.ts`, `tests/{session,grid_canvas,autotest}.spec.ts` |
| shell 集成注入 + review 修 3 bug | `crates/pty/src/shell_inject.rs`, `crates/perga-cli/tests/raw_debug.rs` | `crates/pty/src/lib.rs`, `crates/pty/Cargo.toml`, `Cargo.toml`, `crates/perga-server/src/ws.rs`, `crates/perga-cli/src/{main,raw_debug}.rs`, `scripts/perga-{bash,zsh}.sh` |
| Phase 4 frontend | `render/theme.ts`, `state/{settings,settings_context}.ts`, `input/zoom_shortcuts.ts`, `ui/{modal,settings_panel,context_menu}.tsx`, `tests/{theme,settings,zoom_shortcuts}.spec.ts` | `render/{palette,grid_canvas,block_one,block_list,metrics}.*`, `state/blocks.ts`, `ui/{app,tab_bar,pane_leaf,pane_tree_view}.tsx`, `tests/blocks.spec.ts` |
| 2026-05-23 输入/渲染修补 | `input/paste_shortcuts.ts`, `tests/{paste_shortcuts,metrics}.spec.ts` | `render/{grid_canvas,metrics}.ts*`, `ui/pane_leaf.tsx`, `tests/grid_canvas.spec.ts` |
| Phase 5 backend (transport 提取) | `crates/transport/{Cargo.toml,src/{lib,command,event,size,transport}.rs}` | `Cargo.toml`, `crates/pty/{Cargo.toml, src/{command,event,config,threads,session,shell_inject,lib}.rs, tests/integration.rs}`, `crates/terminal-engine/{Cargo.toml, src/{lib,size,engine,snapshot}.rs}`, `crates/terminal-protocol/{Cargo.toml, src/event.rs}` |
| Phase 5 backend (SSH) | `crates/ssh/{Cargo.toml, src/{lib,config,error,handler,shuttle,session}.rs}`, `crates/perga-cli/src/bin/ssh_probe.rs`, `crates/perga-server/src/{profiles,http}.rs` | `crates/perga-cli/Cargo.toml`, `crates/perga-server/{Cargo.toml, src/{lib,ws}.rs}`, `crates/terminal-session/{Cargo.toml, src/{session,event_loop}.rs, tests/integration.rs}` |
| Phase 5 frontend | `web/src/state/profiles.ts`, `web/tests/profiles.spec.ts` | `web/src/{net/ws,state/workspace}.ts`, `web/src/ui/{app,settings_panel}.tsx` |
| Phase 5.5 CRUD + password | — | `crates/{ssh,perga-server}/src/...`, `web/src/{state/profiles,ui/settings_panel}.ts*`, `web/{tests/profiles.spec,vite.config}.ts` |
| Phase 5.5 review 修复 | — | `crates/ssh/src/session.rs`(bridge shutdown + 单测)、`crates/terminal-protocol/src/{event,encoder}.rs`(SessionError 变体)、`crates/perga-server/src/ws.rs`(upgrade-then-spawn 重构)、`crates/terminal-{session,protocol}/tests/*.rs`(非穷尽 match 修)、`web/src/state/{protocol,session,session_store,profiles}.ts`、`web/src/ui/pane_leaf.tsx`、`web/tests/{session,profiles}.spec.ts` |
| Phase 6 perga-core 提取 | `crates/perga-core/{Cargo.toml, src/{lib,profiles,wire,session_factory}.rs}` | `Cargo.toml`(workspace member)、`crates/perga-server/{Cargo.toml, src/{lib,http,ws}.rs}`(改 thin wrapper)、删除 `crates/perga-server/src/{profiles,wire}.rs` |
| Phase 6 perga-tauri 骨架 | `crates/perga-tauri/{Cargo.toml,build.rs,tauri.conf.json,capabilities/default.json,icons/*.png,src/{main,paths,session_registry,commands/{mod,profiles,session,platform}}.rs}` | `Cargo.toml`(workspace member) |
| Phase 6 前端 transport / ProfileApi 抽象 | `web/src/net/{transport,tauri,index}.ts`、`web/src/util/platform.ts`、`web/tests/platform.spec.ts` | `web/src/net/ws.ts`(导出 `createWsTransport`)、`web/src/state/{workspace,profiles}.ts`、`web/package.json`(@tauri-apps/api + cli) |
| Phase 6 移动端 UX | `web/src/ui/{host_form,profile_picker}.tsx` | `web/src/ui/{settings_panel,tab_bar,app}.tsx`(form 抽出 + + 按钮分支 + first-run picker) |

代码量(粗算):后端 ~600 行(Phase 0/1)+ ~1400 行(Phase 5 含 transport / ssh / server + 测试)+ ~700 行(Phase 6 含 perga-core 重组 + perga-tauri scaffold + 测试),前端 Phase 1 ~1500 行 + Phase 2 ~1100 行 + Phase 3 ~1100 行 + Phase 5 ~200 行 + Phase 6 ~600 行(transport + ProfileApi 抽象 + profile_picker + 平台探测,均含测试)。
